import AuthWrapper from "../../components/AuthWrapper";
import RegisterForm from "../../components/RegisterForm";


const Register = () => {
    return (
        <AuthWrapper FormComponrnt={RegisterForm} />
    )
};

export default Register;
